game_director:
  ros__parameters:
    # Empty arrays ([]) can fail to parse on some ROS 2 distros.
    # Use one of: none|off|disable_all to disable all BOOT checks.
    boot_validation_requirements:
      - none
      # - dispatch_states_publisher
      # - task_summaries_publisher
      # - task_api_requests_subscriber
      # - expected_robots_seen
      # - expected_robots_online
      # - valid_robot_maps
      # - finite_robot_pose
      # - sane_battery_values
